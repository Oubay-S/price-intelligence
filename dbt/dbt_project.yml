name: 'price_intelligence'
version: '1.0.0'
config-version: 2

profile: 'price_intel_bq'

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

models:
  price_intelligence:
    staging:
      +materialized: view
    intermediate:
      +materialized: ephemeral
    mart:
      +materialized: table
