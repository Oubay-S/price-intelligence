[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: tests purement unitaires (pas de service externe)
    integration: tests nécessitant le Bigtable emulator
